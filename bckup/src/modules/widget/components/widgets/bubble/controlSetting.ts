import {ControlSetting} from '@/modules/widget/models';


// eslint-disable-next-line max-statements
export const bubbleControlHandler = () => {
	const setting: ControlSetting = {
		disabled: false,
		options: null,
		show: true
	};

	return setting;
};
